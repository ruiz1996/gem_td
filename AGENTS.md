# 本项目协作约定

用户于2026-09-07明确授权：每轮修改完成后直接推送GitHub，无需再次询问。

- 在必要检查通过后提交本轮修改，推送到 `https://github.com/ruiz1996/gem_td` 的 `main`，沿用已有GitHub Actions发布试玩页面。
- 按完成的一轮修改提交，不要求每个编辑步骤单独推送。用户随后明确要求只做本地修改或暂不发布时，遵循该次要求。
- 根据修改范围验证。代码、界面或配置修改运行相关测试、类型检查和Pages构建；纯文档修改检查内容与差异，无需重复执行本地游戏测试。
- 推送前确认远端和分支，保留远端已有更新，不强制推送。不要提交凭据、依赖目录、构建产物或本地临时文件。
- 若工作区源码与GitHub发布检出分开，同步源码和项目文档到正确的发布检出；确保 `game/` 是普通源码目录，不作为嵌套仓库或子模块提交。
- 推送后确认Actions结果。检查或发布失败时继续排查，并如实说明尚未完成的部分。

公开试玩入口：https://ruiz1996.github.io/gem_td/
